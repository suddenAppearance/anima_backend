version: '3'

services:
  postgres:
    image: postgres:14.2
    ports:
      - "5432:5432"
    env_file:
      - .env
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready" ]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - pg-data:/var/lib/postgresql/data
    logging:
      driver: none

  project-service-db:
    image: postgres:14.2
    ports:
      - "30432:5432"
    env_file:
      - .env
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready" ]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - pg-project-service-data:/var/lib/postgresql/data

    logging:
      driver: none

  animation-service-db:
    image: postgres:14.2
    ports:
      - "31432:5432"
    env_file:
      - .env
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready" ]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - pg-animation-service-data:/var/lib/postgresql/data

    logging:
      driver: none

  file-service-db:
    image: postgres:14.2
    ports:
      - "32432:5432"
    env_file:
      - .env
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready" ]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - pg-file-service-data:/var/lib/postgresql/data

    logging:
      driver: none

  project-service:
    build:
      context: project_service
      dockerfile: Dockerfile
    command: bash -c "alembic upgrade head && uvicorn main:app --host 0.0.0.0 --port 8000 --reload"
    env_file:
      - .env
    environment:
      POSTGRES_HOST: project-service-db
    volumes:
      - ./project_service:/app
    ports:
      - "8000:8000"
    depends_on:
      - project-service-db

  animation-service:
    build:
      context: animation_service
      dockerfile: Dockerfile
    command: bash -c "alembic upgrade head && uvicorn main:app --host 0.0.0.0 --port 8000 --reload"
    env_file:
      - .env
    environment:
      POSTGRES_HOST: animation-service-db
    volumes:
      - ./animation_service:/app
    ports:
      - "8001:8000"
    depends_on:
      - animation-service-db

  file-service:
    build:
      context: file_service
      dockerfile: Dockerfile
    command: bash -c "alembic upgrade head && uvicorn main:app --host 0.0.0.0 --port 8000 --reload"
    env_file:
      - .env
    environment:
      POSTGRES_HOST: file-service-db
    volumes:
      - ./file_service:/app
    ports:
      - "8002:8000"
    depends_on:
      - file-service-db
      - minio


  keycloak:
    depends_on:
      - postgres
    container_name: local_keycloak
    logging:
      driver: none
    environment:
      DB_VENDOR: postgres
      DB_ADDR: postgres
      DB_DATABASE: anima
      DB_USER: postgres
      DB_PASSWORD: qwerty123
    image: jboss/keycloak:latest
    ports:
      - "8787:8080"
    restart: unless-stopped

  minio:
    image: minio/minio:latest
    restart: unless-stopped
    ports:
      - "${MINIO_CONSOLE_PORT}:${MINIO_CONSOLE_PORT}"
    env_file:
      - .env
    command: server --address 0.0.0.0:${MINIO_SERVER_PORT} --console-address 0.0.0.0:${MINIO_CONSOLE_PORT} /data
    volumes:
      - minio-data:/data
    logging:
      driver: none

volumes:
  pg-data:
  pg-project-service-data:
  pg-animation-service-data:
  pg-file-service-data:
  minio-data: